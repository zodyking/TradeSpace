# DO NOT add COMPOSE_FILE=docker-compose.yml:docker-compose.dockploy.yml
# That merge breaks Traefik (404). Use compose file docker-compose.dockploy.yml only.
#
# Dokploy Environment: leave empty or delete all variables, then redeploy.
# Dokploy Domains tab: remove any domain rows (routing is in compose labels).
#
# http://qauntview-quantview-lwersl-74dba7-155-138-143-191.sslip.io
