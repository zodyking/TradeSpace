# Dockploy: set Compose file to docker-compose.dockploy.yml (or rely on repo .env COMPOSE_FILE).
# No other env vars required — domain and backend.env.dockploy are preconfigured.
#
# Open: http://qauntview-quantview-lwersl-74dba7-155-138-143-191.sslip.io
# Domains UI: optional; if used → service frontend, port 80, HTTPS OFF
